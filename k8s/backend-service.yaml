apiVersion: v1
kind: Service
metadata:
  name: medical-backend
  namespace: medical-app
  labels:
    app: medical-backend   
spec:
  selector:
    app: medical-backend   
  ports:
    - name: http           
      protocol: TCP
      port: 8080           
      targetPort: 8080     
  type: ClusterIP          